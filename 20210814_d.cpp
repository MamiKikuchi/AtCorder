#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    vector<long long> u, v, w;
    u.resize(n);
    v.resize(n);
    w.resize(n);
    for(int i=0;i<n;i++) cin >> u[i] >> v[i] >> w[i];
}
